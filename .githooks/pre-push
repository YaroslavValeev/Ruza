#!/usr/bin/env bash

if [ "${ALLOW_GIT_PUSH:-0}" = "1" ]; then
  echo "Push allowed by ALLOW_GIT_PUSH=1 override."
  exit 0
fi

echo "Push blocked by project policy. Set ALLOW_GIT_PUSH=1 only with human approval."
exit 1
